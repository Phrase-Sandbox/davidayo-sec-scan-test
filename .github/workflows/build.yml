name: Build

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: core-arc-ondemand-launchpad

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and push
        uses: Phrase-Engineering/platform-core-reusable-actions/actions/build-test-push-container@main
        with:
          aws-account-map: '{"internal": "333070415076"}'
          ecr-repository: 333070415076.dkr.ecr.eu-west-1.amazonaws.com/image/phrase/phrase-launchpad/phrase-sec-scan/app
          dockerfile-path: Dockerfile
          build-context: .
          additional-tags: "commit-${{ github.sha }}"
          build-args: '[{"key": "BASE_IMAGE_PREFIX", "value": "333070415076.dkr.ecr.eu-west-1.amazonaws.com/image/mirror/docker-hub/library/"}]'
